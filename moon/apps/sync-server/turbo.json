{
  "$schema": "https://turborepo.org/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "dependsOn": ["^build", "build"],
      "passThroughEnv": ["PORT", "API_URL"]
    },
    "build": {
      "env": ["TIPTAP_PRIVATE_REGISTRY_KEY", "NODE_ENV", "PORT", "API_URL"]
    }
  }
}
